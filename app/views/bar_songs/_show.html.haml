.name
  name:
  %b= bar_song.name
.artist
  artist:
  %b= bar_song.artist
.database_ID
  database_ID:
  %b= bar_song.database_ID
.play
  = button_to "Play now!", :action => "vote", :id => bar_song.id, :bar_id => @bar.id, :controller => "bar_songs"